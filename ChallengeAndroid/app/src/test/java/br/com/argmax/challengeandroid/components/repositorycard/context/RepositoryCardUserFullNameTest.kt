package br.com.argmax.challengeandroid.components.repositorycard.context

import org.junit.Before
import org.junit.Test

class RepositoryCardUserFullNameTest : RepositoryCardTest() {

    @Before
    fun `setup scenario`() {
        `when repository card has all data`()
    }

    @Test
    fun `test if user full name is visible`() {
        `assert that user full name is visible`()
    }

    @Test
    fun `test if user full name is set correctly`() {
        `assert that user full name is set correctly`()
    }

}
